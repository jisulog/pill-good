import { observer } from 'mobx-react';
import React, { Component } from 'react';

class BookCancelContainer extends Component {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}

export default observer(BookCancelContainer);